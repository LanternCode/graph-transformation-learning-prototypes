# Graph-Rewriting-Neural-Network

## The first model is DirectedGAEGCN, a graph autoencoder with GCN as the encoder and the bilinear decoder.

## The second model is DirectedGAEGIN, a graph autoencoder with GIN as the encoder and the bilinear decoder.